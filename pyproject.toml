[project]
name = "flagrant-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Michal Buczko", email = "michal@buczko.pl" }
]
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
]

[tool.hatch.build.targets.wheel]
packages = ["src/flagrant"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ipython>=9.16.1",
]
